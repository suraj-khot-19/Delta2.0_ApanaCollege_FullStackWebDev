/*
 repeat(count: number): string
number of copies to append

Returns a String value that is made from count copies appended together. If count is 0, the empty string is returned.
*/
let s = "suraj"
let x = s.repeat(3)
console.log(x) //surajsurajsuraj